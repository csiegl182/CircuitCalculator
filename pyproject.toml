[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "numpy",
    "schemdraw>=0.15",
    "matplotlib>=3.6.0"
    ]

[project]
name = "CircuitCalculator"
description = "blabla"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy"
]

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
e